---
import education from "../../collections/education.json";
import Collapse from "./Collapse.astro";
---

<div class="grid place-items-center bg-base-200">
    <div class="hero bg-base-200 pb-20 md:w-4/5">
        <div class="hero-content">
            <div class="w-100">
                <h1 class="mb-10 text-4xl font-extrabold text-gray-900 animate-fade-in-down md:text-6xl"  >
                    <span class="text-transparent bg-clip-text bg-gradient-to-r from-blue-500 via-blue-600 to-blue-700">EDUCACIÓN</span>
                </h1>
                <p class="py-6 mb-5 text-justify">
                    Soy un desarrollador web con pasión por crear experiencias
                    digitales innovadoras. A lo largo de mi carrera, he
                    adquirido un sólido conocimiento en una variedad de
                    tecnologías. Mi experiencia me permite abordar proyectos de
                    diversa complejidad, desde el desarrollo frontend hasta el
                    backend, siempre buscando soluciones eficientes y
                    escalables.
                </p>
                <div class="mb-10">
                    <ul class="timeline timeline-vertical">
                        {
                            education.map((e) => {
                                return (
                                    <li>
                                        <div class="timeline-start mr-2">{e.dateF}</div>
                                        <div class="timeline-middle">
                                                {e.status === 1 && (
                                                    <i
                                                        class="fa-solid fa-circle-check"
                                                        style="color: #03c200;"
                                                    />
                                                )}
                                                {e.status === 2 && (
                                                    <i
                                                        class="fa-solid fa-hourglass-half"
                                                        style="color: #FFD43B;"
                                                    />
                                                )}
                                            </div>
                                        <div class="timeline-end timeline-box">
                                           {e.degree}
                                        </div>
                                        <hr />
                                    </li>
                                );
                            })
                        }
                    </ul>
                </div>
                {
                    education.map((e) => {
                        return (
                            <div id=`collapse${e.id}` class="mb-5">
                                <Collapse id={e.id}/>
                            </div>
                        );
                    })
                }
            </div>
        </div>
    </div>
</div>
